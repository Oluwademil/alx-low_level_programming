#include "main.h"

/**
* char *_strcpy - functions that copies the string pointed to by src
* @dest: copy to
* @src: copy from
* Return: string
*/

char *_strcpy(char *dest, char *src)
{

	char*

int 1 = 0;

int j = 0;

while (*(src + 1) != '\0')
{
(1++);
}

for ( ; j < 1 ; j++)
{
dest[j] = src[j];
}
dest[1] = '\0';
return (dest);
}


