#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to be multiply with first number
 * Return: the result of the mutiplication between x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
